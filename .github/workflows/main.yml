name: Stock News Bot Auto Run

on:
  schedule:
    - cron: '*/30 * * * *'  # 30분마다 자동 실행
  workflow_dispatch:      # 수동 실행 버튼

jobs:
  run-stock-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write     # 봇이 파일을 쓸 수 있게 허락

    steps:
      - name: 코드 가져오기
        uses: actions/checkout@v3

      - name: 파이썬 설치
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'  # 최신 버전으로 상향 (에러 방지)

      - name: 라이브러리 설치 (업그레이드)
        run: |
          python -m pip install --upgrade pip
          pip install -U feedparser tweepy google-generativeai

      - name: 봇 실행
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          CONSUMER_KEY: ${{ secrets.CONSUMER_KEY }}
          CONSUMER_SECRET: ${{ secrets.CONSUMER_SECRET }}
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          ACCESS_TOKEN_SECRET: ${{ secrets.ACCESS_TOKEN_SECRET }}
        run: python bot.py

      - name: 기억장치 저장 (Commit & Push)
        run: |
          git config --global user.name "StockNewsBot"
          git config --global user.email "bot@noreply.github.com"
          # 파일 4개(미국3 + 한국1) 모두 저장
          git add last_link_us_investing.txt last_link_us_finance.txt last_link_us_tech.txt last_link_kr.txt 2>/dev/null || true
          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 뉴스 처리 완료" && git push)